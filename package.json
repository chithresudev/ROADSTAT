{
  "name": "roadstat",
  "version": "1.0.0",
  "description": "ROADSTAT APP",
  "main": "index.js",
  "scripts": {
    "dev": "concurrently \"npm run server\" \"npm run client\"",
    "server": "cd Backend && nodemon server.js",
    "client": "cd Frontend && npx vite",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "concurrently": "^8.2.2"
  }
}

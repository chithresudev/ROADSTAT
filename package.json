{
  "name": "roadstat",
  "version": "1.0.0",
  "description": "ROADSTAT FULLSTACK APP",
  "main": "index.js",
  "scripts": {
    "dev": "concurrently \"npm run server\" \"npm run client\"",
    "server": "cd Backend && nodemon server.js",
    "client": "cd Frontend && vite --host",
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "cd Frontend && vite build"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "concurrently": "^9.1.0",
    "nodemon": "^2.0.22",
    "vite": "^4.4.9"
  }
}
